[template]
src = "index-search.toml"
dest = "/etc/nginx/conf.d/index-search.api.tomtop.com.conf"
keys = [
    "/index-search"
]
check_cmd = "/usr/sbin/nginx -t"
reload_cmd = "/usr/sbin/nginx -s reload"
